let dog = {
  name: 'Spot',
  numLegs: 4,
}
// Only change code below this line
const logName = console.log(dog.name)
const logLegs = console.log(dog.numLegs)

module.exports = { logName, logLegs, dog }
